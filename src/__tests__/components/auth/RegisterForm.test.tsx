describe('Register Form', () => {

	describe('Validation', () => {
		it('Password passes validation', () => {})
		it('Password fails validation', () => {})
		it('Email passes validation', () => {})
		it('Email fails validation', () => {})
	})

	describe('Create User Request', () => {
		it('User Created successfully', () => {})
		it('Failed to create user', () => {})
	})

    it('Sing In link takes the user to the Login form', () => {})
})
